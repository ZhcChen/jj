# fund-monitor

基金监控核心模块。

当前技术栈：
- Rust

本模块当前职责：
- 基金数据源接入
- 监控规则引擎
- 定时任务
- 告警通知
- 持久化存储

说明：
- 本模块不再提供 Web UI。
- 当前可执行入口是后台轮询进程，会按配置周期抓取基金数据并执行规则。
- 所有前端与交互界面后续统一在 `modules/app` 的 Flutter 桌面端模块继续开发。
